---
import H2hPairView from '../../views/H2hPairView.astro';
import { buildH2hPairs } from '../../lib/h2h.js';

export function getStaticPaths() {
  return buildH2hPairs().map((p) => ({ params: { pair: p.slug }, props: { p } }));
}
const { p } = Astro.props;
---
<H2hPairView p={p} lang="en" />
