---
import H2hHubView from '../../../views/H2hHubView.astro';
import { H2H_LEAGUES } from '../../../lib/h2h.js';

export function getStaticPaths() {
  return Object.keys(H2H_LEAGUES).map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params;
---
<H2hHubView lang={lang} />
