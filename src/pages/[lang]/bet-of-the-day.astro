---
import BetOfDayView from '../../views/BetOfDayView.astro';
import { ALT_LANGS } from '../../i18n/index.js';

export function getStaticPaths() {
  return ALT_LANGS.map((lang) => ({ params: { lang } }));
}
const { lang } = Astro.params;
---
<BetOfDayView lang={lang} />
